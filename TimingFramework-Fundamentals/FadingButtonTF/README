Use:

  mvn initialize # install the local TimingFramework.jar,etc in local repo
  mvn package # compile and generates JAR file
  mvn exec:java # execute the main class

to compile and run the example
